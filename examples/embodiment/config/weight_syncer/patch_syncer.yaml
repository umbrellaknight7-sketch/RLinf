# type: patch
# patch:
#   snapshot_device: cpu
#   delta_encoding: True
#   compression: none

#   init_sync:
#     enabled: True
#     prefixes: null
#     bucket_size: 134217728

type: patch

patch:
  snapshot_device: cpu
  transport_device: cpu
  delta_encoding: True
  compression: none

  init_sync:
    enabled: True
    prefixes:
      - starvla_model.action_model
      - value_head
      - actor_logstd
      - reinflow_explore_noise_net
    bucket_size: 33554432